# Leave blank for the built-in demo transport.
# Production should point this at the Corvis application API; PDF bytes are uploaded
# directly to object storage using presigned multipart URLs returned by this API.
NEXT_PUBLIC_CORVIS_API_BASE=
NEXT_PUBLIC_CORVIS_MOCK_API=true
